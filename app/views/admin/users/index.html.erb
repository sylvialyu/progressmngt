<h2 class="text-center">我的学生</h2>

<div class="pull-right">
  <%= link_to("新增学生", new_admin_user_path, class: "btn btn-sm btn-primary") %>
</div>

<div style="margin-top: 120px;">
  <div class="row">
    <% @users.each do |user| %>
      <div class="col-md-4">
        <%= link_to(user.display_name, admin_user_path(user), class: "btn btn-lg btn-info", style: "margin-bottom: 30px; width: 120px;") %>
      </div>
      <% end %>
  </div>
</div>
